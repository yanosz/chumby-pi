# IDE state
.idea/

# Local-only investigation notes (device identities, GUIDs, anything
# tied to the user's own hardware) — never committed.
/claude-docs/private/

# Deb build staging and output (pkg/build-debs.sh). The staged data deb
# contains the copyrighted SWFs and tones — never publish it.
/pkg/build/
/pkg/out/

# Copyrighted chumby material. The player's own copies (controlpanel.swf,
# widget SWFs, thumbnails, alarm tones, the decompiled panel) live in the
# ruffle/ submodule and are gitignored there. Kept here as a backstop.
*.swf
*.mp3
*.wav

/resources/zurks-offline-firmware-classic/
__pycache__/

# Downloaded reference schematics (large, fetch on demand)
pdf/
